经验酒壶满界面={}

function 经验酒壶满界面.取消()
	sysLog('经验酒壶满了！')
	local 不再提醒={x=-1,y=-1}
	不再提醒=操作.识别(按钮.经验酒壶界面_不再提醒)
	if 不再提醒.x==-1 then
		操作.点击({x=500,y=321})
		mSleep(300)
	end
	local 取消={x=475,y=390}
	操作.点击(取消)
	return 探索关卡界面.to战斗准备界面()
end